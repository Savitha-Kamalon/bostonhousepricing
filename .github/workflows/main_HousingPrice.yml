# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - HousingPrice

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Log in to registry
      uses: docker/login-action@v1
      with:
        registry: https://houseprice.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_27f93cbbe45346588574c309e722c339 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_220636403153433cbcb8cb9ebb560bfe }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v2
      with:
        push: true
        tags: houseprice.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_27f93cbbe45346588574c309e722c339 }}/hello-express:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'HousingPrice'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_dc1e137e77a34aaa81e7d54417ee09b0 }}
        images: 'houseprice.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_27f93cbbe45346588574c309e722c339 }}/hello-express:${{ github.sha }}'